# @package _global_
defaults:
  - override /data: rdb7_cgr
  - override /model: dmpnn
  - override /scheduler: cosine_with_warmup
  - override /optimizer: adamw_gps

wandb: true
mode: train
project_name: CGR_DMPNN
seed: 0
use_cuda: true
device: gpu

save_model: false
model_path: models/experiment1
pretrained_path: null

epochs: 1000

model:
  shared_weights: false
  # pool_cfg:
  #   aggr: add

num_node_features: ???
num_edge_features: ???

hydra:
  output_subdir: null
  run:
    dir: .

