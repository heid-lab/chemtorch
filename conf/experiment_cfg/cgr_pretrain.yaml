# @package _global_
defaults:
  - override /data: rdb7_cgr_with_pretransform
  - override /model: gps
  - override /scheduler: cosine_with_warmup
  - override /optimizer: adamw_gps

wandb: true
mode: train
project_name: unmapped_pretrain
seed: 0
use_cuda: true
device: gpu

save_model: true
model_path: unmapped_pretrained

epochs: 2

model:
  shared_weights: false
  pool_cfg:
    aggr: add

num_node_features: ???
num_edge_features: ???

hydra:
  output_subdir: null
  run:
    dir: .

