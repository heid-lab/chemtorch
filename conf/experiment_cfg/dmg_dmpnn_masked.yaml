# @package _global_
defaults:
  - override /data: rdb7_dmg
  - override /model: dmpnn_masked
  - override /scheduler: cosine_with_warmup
  - override /optimizer: adamw_gps

wandb: true
mode: train
project_name: dmpnn_masked
seed: 0
use_cuda: true
device: gpu

scheduler:
  scheduler:
    lr_lambda:
      num_warmup_steps: 8
      
save_model: false
model_path: models/experiment1

epochs: 300
patience: 50

model:
  shared_weights: false
  # pool_cfg:
  #   aggr: add

num_node_features: ???
num_edge_features: ???

hydra:
  output_subdir: null
  run:
    dir: .

