# @package _global_
defaults:
  - override /data: e2_cgr_with_pretransform_dummy
  - override /model: gps
  - override /scheduler: cosine_with_warmup
  - override /optimizer: adamw_gps

wandb: false
mode: train
project_name: PE_RWSE
seed: 0
use_cuda: true
device: gpu

save_model: false
model_path: models/experiment1

epochs: 1000

data:
  featurizer_cfg:
    atom_featurizer: atom_atomic_num
    bond_featurizer: bond_bond_type

model:
  shared_weights: false
  # pool_cfg:
  #   aggr: add

num_node_features: ???
num_edge_features: ???

hydra:
  output_subdir: null
  run:
    dir: .

