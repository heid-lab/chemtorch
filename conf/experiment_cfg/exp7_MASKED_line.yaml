# @package _global_
defaults:
  - override /data: rdb7_line_dmg_pos
  - override /model: masked_dmg_line
  - override /task_cfg: regression

wandb: true
project_name: exp7
seed: 0
use_cuda: true
device: gpu

under_parameters: null
group_name: masked
run_name: null
      
use_loaded_model: false
pretrained_path: null

task_cfg:
  epochs: 200
  patience: 30

  scheduler_cfg:
    scheduler:
      lr_lambda:
        num_warmup_steps: 10

num_node_features: ???
num_edge_features: ???

hydra:
  output_subdir: null
  run:
    dir: .

