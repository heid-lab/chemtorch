_target_: deepreaction.layer.gnn_block.GNNBlockLayer

defaults:
  - mpnn: gatedgcn_layer

use_residual: false   # MPNN layer has its own residual
use_ffn: false
dropout: 0.0
act: relu
norm: null
hidden_channels: ${model.gnn.layer.hidden_channels}