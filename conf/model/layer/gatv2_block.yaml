_target_: deepreaction.layer.gnn_block.GNNBlockLayer

defaults:
  - mpnn: gatv2_layer

use_residual: true
use_ffn: true
dropout: 0.05
act: relu
norm: null
hidden_channels: ${model.layer.hidden_channels}
