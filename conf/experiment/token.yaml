# @package _global_
defaults:
  - /data_module/data_pipeline: rdb7_fwd
  - /data_module/representation: token
  - /data_module/dataloader_factory: torch_dataloader
  - /model: han
  - /routine: regression
  - /props/label_mean@props.label_mean
  - /props/label_std@props.label_std
  - /props/vocab_size@props.vocab_size
  - _self_

# LOGGING (Weight and Biases)
log: true
project_name: chemtorch
group_name: token
run_name: null

# INITIALIZATION
seed: 0

tasks:
  - fit
  - test

num_classes: 1
 
trainer:
  max_epochs: 100

# MODEL LOADING
load_model: false
ckpt_path: null