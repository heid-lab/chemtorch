_target_: deeprxn.data.dataset.mol_graph_dataset.MolGraphDataset

data_path: "data/barriers_sn2"

input_column: "AAM"
target_column: "ea" 

subsample: null   # TODO: Remove if not used

data_pipeline_cfg:
  _target_: deeprxn.data.DataPipeline
  modules:
    - single_csv_reader
    - ration_splitter