_target_: deeprxn.data.construct_loader
_recursive_: False

defaults:
  - dataset_cfg: rdb7
  - representation_cfg: cgr
  # TODO: Move sample_transform_cfg and dataset_transform_cfg to dataset_cfg
  - sample_transform_cfg: null      # Specify list of sample_transforms
  - dataset_transform_cfg: null     # Specify list of dataset_transforms
  - _self_

batch_size: 50
num_workers: 0