_target_: deepreaction.dataset.mol_graph_dataset.MolGraphDataset
_partial_: true

# === DATA PIPELINE ===================================
defaults:
  # Data pipeline components in chronological order
  - data_source: split_csv_source
  - preprocessing/column_mapper: column_filter_rename
  - _self_ 

# === DATA PIPELINE COMPONENTS ========================
# split_csv_source
data_folder: "deepreaction_datasets/data/rgd1/barriers"
# column_filter_rename
column_mapping:
  smiles: "smiles"
  label: "ea"

# === DATASET =========================================
# Additional dataset arguements (see MolGraphDataset for details)
# subsample: null         
# cache_graphs: true 
# max_cache_size: null
# preprocess_all: true